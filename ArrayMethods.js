//Métodos ARRAY
//Declara tu nombre completo en una array y muéstralo por pantalla separando cada letra por "/"

const nameArray = ["J", "A", "V", "I", "E", "R"];
    function myName(nameArray){
        return nameArray.join("/");
    }
    const resultArray = myName(name);
    console.log(result);